# Roasters Map Todo

-   reverse geocode out the address to replace the long/lat lines.
-   set the map to always be full page screen view height.
-   round the map corners.
-   make the grid list cards bigger to encompass the data without breaking.
-   add the phone number of the location.
-   include a little icon on the grid list map view and cards that indicates directions and have it open google maps.
